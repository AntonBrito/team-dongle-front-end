<!-- pages template -->
{{#each pages as |page|}}
  <div id="pages-content">
    <h1>{{page.title}}</h1>
    <h2>{{page.heading}}</h2>
    <p>{{page.body}}</p>
    <p>{{page.footer}}</p>
  </div>
{{/each}}
